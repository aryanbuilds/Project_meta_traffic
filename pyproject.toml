[project]
name = "openenv-selfdriving-collision-avoidance"
version = "0.1.0"
description = "OpenEnv self-driving collision-avoidance environment for Round-1 hackathon"
requires-python = ">=3.10"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "requests",
  "openai",
  "python-dotenv",
]
